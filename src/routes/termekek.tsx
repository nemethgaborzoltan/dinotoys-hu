import { createFileRoute } from '@tanstack/react-router'
import { useMemo } from 'react'
import { z } from 'zod'
import { ProductCard } from '../components/ProductCard'
import { categories as demoCategories, products as demoProducts } from '../data/products'
import { getCatalogData } from '../server/storefront'
const searchSchema = z.object({ category: z.string().optional(), q: z.string().optional(), age: z.coerce.number().optional(), max: z.coerce.number().optional(), sort: z.string().optional() })
export const Route = createFileRoute('/termekek')({ validateSearch: (search) => searchSchema.parse(search), loader: () => getCatalogData(), component: Products })
function Products() { const search = Route.useSearch(); const navigate = Route.useNavigate(); const data = Route.useLoaderData(); const products = data?.products?.length ? data.products : demoProducts; const categories = data?.categories?.length ? data.categories : demoCategories; const filtered = useMemo(() => { let result = products.filter((p) => { if (search.category && p.category !== search.category) return false; if (search.age && p.ageFrom > search.age) return false; if (search.max && p.retailPrice > search.max) return false; if (search.q && !`${p.name} ${p.brand} ${p.tags.join(' ')}`.toLowerCase().includes(search.q.toLowerCase())) return false; return true }); if (search.sort === 'price-asc') result = [...result].sort((a,b) => a.retailPrice-b.retailPrice); if (search.sort === 'price-desc') result = [...result].sort((a,b) => b.retailPrice-a.retailPrice); if (search.sort === 'rating') result = [...result].sort((a,b) => b.rating-a.rating); if (search.sort === 'trending') result = [...result].sort((a,b) => Number(Boolean(b.trending))-Number(Boolean(a.trending))); return result }, [search]); const set = (patch: Record<string, unknown>) => navigate({ search: (prev) => ({ ...prev, ...patch }) }); return <div className="container section catalog-page"><div className="catalog-title"><div><span className="eyebrow">DinoToys katalógus</span><h1>{search.category || 'Minden játék'}</h1><p>{filtered.length} termék a demo katalógusban</p></div><div className="catalog-actions"><select value={search.sort || ''} onChange={(e)=>set({sort:e.target.value || undefined})}><option value="">Ajánlott sorrend</option><option value="trending">Trend</option><option value="rating">Legjobbra értékelt</option><option value="price-asc">Ár: növekvő</option><option value="price-desc">Ár: csökkenő</option></select></div></div><div className="catalog-layout"><aside className="filters"><div className="filter-block"><b>Keresés</b><input value={search.q || ''} onChange={(e)=>set({q:e.target.value || undefined})} placeholder="Pl. Stitch"/></div><div className="filter-block"><b>Kategória</b><button className={!search.category?'selected':''} onClick={()=>set({category:undefined})}>Mind</button>{categories.map(c=><button key={c.name} className={search.category===c.name?'selected':''} onClick={()=>set({category:c.name})}>{c.icon} {c.name}</button>)}</div><div className="filter-block"><b>Életkor</b><div className="filter-chips">{[3,4,6,8,10].map(age=><button key={age} className={search.age===age?'selected':''} onClick={()=>set({age:search.age===age?undefined:age})}>{age}+ év</button>)}</div></div><div className="filter-block"><b>Maximum ár</b><div className="filter-chips">{[3000,5000,8000,10000].map(max=><button key={max} className={search.max===max?'selected':''} onClick={()=>set({max:search.max===max?undefined:max})}>{max/1000}k</button>)}</div></div></aside><div>{filtered.length ? <div className="product-grid catalog-grid">{filtered.map(p=><ProductCard key={p.id} product={p}/>)}</div> : <div className="empty-state large"><span>🧩</span><h3>Erre most nincs találat</h3><p>Törölj egy szűrőt, vagy próbálj tágabb keresést.</p><button className="btn btn-primary" onClick={()=>navigate({search:{}})}>Szűrők törlése</button></div>}</div></div></div> }
